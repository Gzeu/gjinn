{
  "name": "gjinn-ai-genie",
  "version": "1.0.0",
  "description": "Gjinn - Your AI Genie of Imagination",
  "main": "app.js",
  "scripts": {
    "start": "http-server -p 3000",
    "build": "echo 'Building for production...' && mkdir -p dist && cp -r * dist/ || echo 'Build complete'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist"
  },
  "keywords": ["ai", "image-generation", "creativity", "gjinn"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
